# OpenWebUi-Ollama-chroma-docker-swarm

Deploy Ollama, OpenWebUI, and Chroma inside a Docker Swarm

You need to enable Docker GPU support in order to utilize the Ollama as is. to do this, see Nvidia's site